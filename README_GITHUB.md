# 🚀 AeroCalc 部署至 GitHub Pages 指南

按照以下步骤，您可以将此应用托管到 GitHub，并在 iPad 上作为原生 App 安装。

## 第一步：创建 GitHub 仓库
1. 登录您的 [GitHub](https://github.com/) 账号。
2. 点击右上角的 **+** 号，选择 **New repository**。
3. 命名为 `aerocalc`（或者您喜欢的任何名称）。
4. 选择 **Public**。
5. 点击 **Create repository**。

## 第二步：上传代码
如果您在本地使用 Git，请执行：
```bash
git init
git add .
git commit -m "Initial AeroCalc release"
git branch -M main
git remote add origin https://github.com/您的用户名/aerocalc.git
git push -u origin main
```
*或者，您也可以在 GitHub 页面上点击 "uploading an existing file"，将项目文件夹中的所有文件拖进去。*

## 第三步：开启 GitHub Pages 托管
1. 进入 GitHub 仓库的 **Settings** 选项卡。
2. 在左侧菜单中点击 **Pages**。
3. 在 **Build and deployment** 下的 **Branch** 栏目，选择 `main` 分支和 `/(root)` 文件夹。
4. 点击 **Save**。
5. 等待 1-2 分钟，GitHub 会在上方显示：*"Your site is live at ..."*。

## 第四步：在 iPad 上安装
1. 在 iPad 上使用 **Safari 浏览器** 打开上述 GitHub 提供的 URL。
2. 点击 Safari 右上角的 **分享 (Share)** 按钮（向上箭头的方块图标）。
3. 在弹出的菜单中向下滚动，选择 **添加到主屏幕 (Add to Home Screen)**。
4. 点击 **添加 (Add)**。
5. 现在，桌面上会出现 AeroCalc 图标。点击它，应用将以**全屏模式**启动，且**支持离线使用**。

---
*注：本应用使用 HashRouter 模式，完美兼容 GitHub Pages 的路径跳转。*
